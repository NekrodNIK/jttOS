[package]
name = "kernel"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
paste = "1.0.15"

[package.metadata.build-script]
rustc-target = "i386-unknown-none"
